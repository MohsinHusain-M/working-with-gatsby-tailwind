import React from "react";
import Layout from "../components/Layout";

// markup
const NotFoundPage = () => {
  return (
    <Layout>
      <h1>ERROR</h1>
    </Layout>
  );
};

export default NotFoundPage;
